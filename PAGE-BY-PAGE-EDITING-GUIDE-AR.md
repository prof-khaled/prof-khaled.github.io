# دليل تعديل صفحات الموقع صفحةً بصفحة

## قاعدة العمل العامة

1. أنشئ نسخة احتياطية قبل أي تعديل.
2. لا تغيّر اسم ملف HTML منشور إلا عند وجود ضرورة وخطة تحويل روابط.
3. عدّل النص الإنجليزي والعربي معًا.
4. احتفظ بعناصر `<span class="en">` و`<span class="ar">`.
5. ضغط الصور إلى WebP قبل رفعها.
6. اختبر الروابط واللغة والهاتف بعد كل تعديل.

## الملفات المشتركة

| الوظيفة | الملف |
|---|---|
| الرأس والتذييل والقائمة | `assets/site-header-footer-v9.css` و`assets/v13-platform.js` |
| تحسينات الإصدار الحالي | `assets/v14-refinements.css` |
| اللغة والقائمة | `assets/v13-platform.js` |
| إعداد Supabase العام | `assets/supabase-config.js` |
| بيانات Knowledge Hub | `assets/data/knowledge-hub-resources.json` |

## 1. الصفحة الرئيسية

**الملف:** `index.html`

يمكن تعديل:

- العنوان التعريفي داخل `<section class="hero">`.
- الصورة الرئيسية داخل `figure.portrait`.
- الروابط السريعة إلى صفحات About والبحث والمنشورات.
- بطاقات المجالات البحثية.

الصورة الحالية:

```text
images/prof-khaled-home-portrait-v14.webp
```

أبعاد مقترحة: 800 × 1000 بكسل، WebP، أقل من 200KB.

## 2. About

**الملف:** `01-about-prof-khaled.html`

- عدّل السيرة والملخص الأكاديمي.
- راجع قسم `On this page / محتويات هذه الصفحة` بعد تغيير أي معرف `id`.
- لا تغيّر البيانات الأكاديمية دون مصدر موثق.

## 3. المسيرة الأكاديمية والتعليم

**الملف:** `02-academic-career-education.html`

- أضف الوظائف والمؤهلات والزيارات العلمية داخل التسلسل الزمني.
- أضف صورة إلى `images/academic-career/` بصيغة WebP.
- تمت إزالة التذييل القديم المكرر؛ لا تضف Footer آخر قبل التذييل الموحد.

## 4. البحث والخبرة العلمية

**الملف:** `03-research-scientific-expertise.html`

- حدّث المجالات البحثية والوصف العلمي.
- اربط كل مجال بالمشروعات والمنشورات ذات الصلة.
- تجنب أرقام إنجازات غير موثقة.

## 5. المنشورات

**الملف:** `04-publications.html`

**البيانات:**

```text
assets/data/publications.json
```

عند إضافة منشور:

- العنوان.
- المؤلفون.
- المجلة.
- السنة.
- المجلد والصفحات.
- DOI إن وجد.
- رابط الناشر الرسمي.

لا تضف ملف PDF محميًا دون تصريح.

## 6. المشروعات البحثية

**الملف:** `05-research-projects.html`

**البيانات:** `assets/data/research-projects.json`

أضف المشروع في JSON ثم راجع ظهوره في المرشحات.

## 7. المجموعة البحثية

**الملف:** `06-research-group.html`

- أضف أعضاء المجموعة فقط بعد الموافقة على عرض البيانات.
- استخدم البريد المؤسسي عند الحاجة.
- لا تنشر أرقام هواتف أو بيانات شخصية غير لازمة.

## 8. التدريس والمقررات

**الملف:** `07-teaching-courses.html`

- أضف اسم المقرر والمستوى والوصف ونواتج التعلم.
- المواد المحمية لا توضع داخل مجلد `documents` العام.
- تمت إزالة التذييل القديم المكرر.

## 9. الكتب والنشاط العلمي

**الملف:** `08-books-scientific-activity.html`

**البيانات:** `assets/data/books.json`

- ضع صورة الغلاف داخل `images/books/`.
- سجل بيانات النشر بدقة.
- لا ترفع نسخة كاملة من كتاب محمي دون إذن.

## 10. الجوائز والإنجازات

**الملف:** `09-awards-achievements.html`

- أضف صورة الدليل إلى `images/awards/`.
- اكتب اسم الجائزة والجهة والسنة كما في الوثيقة.
- لا تستخدم وصفًا تسويقيًا يتجاوز ما تثبته الشهادة.

## 11. مركز تدريب علوم وهندسة التآكل

**الملف:** `10-scientific-services-training.html`

- الصفحة تستخدم الاسم الرسمي بالعربية والإنجليزية.
- الصور المحسنة موجودة في `images/services/v14/`.
- الصورة العربية الرئيسية: `corrosion-center-hero-ar.webp`.
- أضف صورًا جديدة إلى مواضع `data-center-image-slot`.
- حدّث الدورات والخدمات والاستشارات من المصدر الرسمي فقط.

## 12. Knowledge Hub

**الملف:** `knowledge-hub.html`

**البيانات:** `assets/data/knowledge-hub-resources.json`

- لا تحتاج إلى تعديل HTML لكل بطاقة.
- أضف سجل JSON جديد ثم اختبر البحث والمرشحات.
- استخدم `access: public` أو `registered` أو `enrolled`.
- لا تربط موردًا محميًا بملف عام.

## 13. التسجيل

**الملف:** `auth.html`

- زر Google هو المسار الرئيسي.
- لا تضع Client Secret في الصفحة.
- النصوص ورسائل الخطأ موجودة في `assets/knowledge-auth-page.js`.

## 14. لوحة الطالب

**الملف:** `knowledge-dashboard.html`

**البرمجة:** `assets/knowledge-dashboard.js`

- تعرض المقررات والتسجيلات من Supabase.
- أي قسم جديد يجب أن يلتزم بصلاحيات RLS.

## 15. لوحة الإدارة

**الملف:** `knowledge-admin.html`

**البرمجة:** `assets/knowledge-admin.js`

- لا تفتح للعامة.
- اختبر دور المدرس ومدير المحتوى والمدير كل على حدة.

## 16. التعاون

**الملف:** `collaboration.html`

- أضف الجهات أو أنواع التعاون المثبتة.
- لا تعرض شعار مؤسسة دون إذن مناسب.

## 17. التواصل

**الملف:** `contact.html`

- حدّث البريد والهاتف في كل موضع، بما في ذلك التذييل.
- اختبر روابط `mailto:` و`tel:` وWhatsApp.

## 18. المدونة

**الملف:** `blog.html`

- تمت إزالة التذييل القديم المكرر.
- المقالات المستقبلية يمكن تنظيمها داخل `content/news/` أو صفحات مستقلة.
- استخدم تاريخ نشر حقيقيًا ولا تستبدله بتاريخ تحديث التصميم.

## 19. الأدوات الأكاديمية

**الملف:** `academic-tools.html`

- تأكد من عمل كل أداة قبل عرضها.
- الروابط الخارجية يجب أن تستخدم HTTPS و`noopener noreferrer`.

## 20. إمكانية الوصول

**الملف:** `accessibility.html`

- يحتوي على بيان ثنائي اللغة فعلي.
- حدّث تاريخ المراجعة بعد كل تدقيق كبير.

## 21. الخصوصية وشروط الاستخدام

- `privacy.html`
- `terms.html`

حدّثهما قبل جمع أي بيانات جديدة أو إضافة خدمة تحليل أو إرسال خارجي.

## 22. صفحة 404

**الملف:** `404.html`

اختبرها بعد النشر، وتأكد من أن روابط العودة تعمل.

## فحص ما بعد التعديل

شغّل:

```bash
npm install
npm run check
```

أو نفّذ فحص الروابط الموجود في:

```text
tests/check-links.mjs
```

---

# تحديث V15: الصور والقوائم وقسم «في هذه الصفحة»

## تعديل القائمة الرئيسية

القائمة العامة موجودة داخل كل صفحة HTML في العنصر:

```html
<nav id="kfk-main-nav" class="kfk-nav">
```

لا تضف رابط `Collaboration` في المستوى الرئيسي؛ الرابط موجود داخل قائمة `About`. اترك ملف `collaboration.html` دون حذف للحفاظ على الروابط القديمة.

## تغيير صورة Home

استبدل الملف:

```text
images/prof-khaled-home-profile-v15.webp
```

بصورة WebP بنسبة 5:4، مع الاحتفاظ بالاسم نفسه لتجنب تعديل HTML. لا تقص الصورة يدويًا؛ القواعد الحالية تعرضها باستخدام `object-fit: contain`.

## تغيير صور مركز التدريب

الملفات المستخدمة في V15 موجودة داخل:

```text
images/services/v15/
```

يفضل أن تكون جميع الصور 1280×720. استبدال الملف مع الاحتفاظ باسمه يحدّث الموقع دون تعديل الصفحة.

## قسم «في هذه الصفحة»

القسم التلقائي يُنشأ من العناوين `h2` بواسطة:

```text
assets/website-tools.js
```

أما التنسيق والوضوح والألوان ففي:

```text
assets/v15-refinements.css
```

لا تكتب عنوانًا إنجليزيًا منفردًا داخل النسخة العربية؛ استخدم دائمًا عنصري `.en` و`.ar` عند إضافة قسم ثابت.
