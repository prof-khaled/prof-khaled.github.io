PUBLICATIONS PAGE — FIXED PACKAGE

This version fixes the local-file JSON error and synchronizes the footer with index.html.

Installation:
1. Replace the old 04-publications.html with the file in this package.
2. Replace assets/publications.js and assets/publications.css.
3. Keep/copy assets/global-site.css and assets/site-public.js.
4. Keep data/publications.json in the data folder.
5. Keep the two publication banner images in the main website folder.

The HTML now embeds a fallback copy of all 113 publication records, so the page works when opened directly and when served online.
