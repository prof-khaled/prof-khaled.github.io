export default async(req)=>{const doi=new URL(req.url).searchParams.get('doi');if(!doi)return new Response('Missing DOI',{status:400});const r=await fetch('https://api.crossref.org/works/'+encodeURIComponent(doi),{headers:{'User-Agent':'ProfKhaledWebsite/1.0 (mailto:Khaledrice2003@edu.asu.edu.eg)'}});return new Response(await r.text(),{status:r.status,headers:{'content-type':'application/json','access-control-allow-origin':'*'}})}