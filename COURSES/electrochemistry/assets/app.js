(() => {
'use strict';
const body=document.body,sidebar=document.getElementById('sidebar'),menuToggle=document.getElementById('menuToggle'),langSelect=document.getElementById('langSelect'),searchInput=document.getElementById('courseSearch'),searchStatus=document.getElementById('searchStatus'),backTop=document.getElementById('backTop');
const moduleButtons=[...document.querySelectorAll('.complete-btn')],bookmarkButtons=[...document.querySelectorAll('.bookmark-btn')],moduleIds=moduleButtons.map(b=>b.dataset.module),progressFill=[...document.querySelectorAll('.progress-fill')],progressText=[...document.querySelectorAll('.progress-text')];
const store={get(k,d){try{const v=localStorage.getItem(k);return v===null?d:JSON.parse(v)}catch{return d}},set(k,v){try{localStorage.setItem(k,JSON.stringify(v))}catch{}}};
function applyLang(v){body.dataset.lang=v;document.documentElement.lang=v==='ar'?'ar':'en';document.documentElement.dir=v==='ar'?'rtl':'ltr';store.set('ec-lang',v)}
const savedLang=store.get('ec-lang','both');if(langSelect)langSelect.value=savedLang;applyLang(savedLang);langSelect?.addEventListener('change',()=>applyLang(langSelect.value));
menuToggle?.addEventListener('click',()=>{const o=sidebar.classList.toggle('open');menuToggle.setAttribute('aria-expanded',String(o))});sidebar?.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>sidebar.classList.remove('open')));
document.getElementById('fontUp')?.addEventListener('click',()=>{const x=parseFloat(getComputedStyle(document.documentElement).getPropertyValue('--font-scale'))||1;document.documentElement.style.setProperty('--font-scale',Math.min(1.3,x+.05))});
document.getElementById('fontDown')?.addEventListener('click',()=>{const x=parseFloat(getComputedStyle(document.documentElement).getPropertyValue('--font-scale'))||1;document.documentElement.style.setProperty('--font-scale',Math.max(.85,x-.05))});document.getElementById('contrastToggle')?.addEventListener('click',()=>body.classList.toggle('high-contrast'));document.getElementById('printBtn')?.addEventListener('click',()=>window.print());document.getElementById('pdfBtn')?.addEventListener('click',()=>window.print());
const completed=new Set(store.get('ec-completed',[])),bookmarks=new Set(store.get('ec-bookmarks',[]));
function updateProgress(){moduleButtons.forEach(btn=>{const id=btn.dataset.module,done=completed.has(id);btn.classList.toggle('done',done);btn.setAttribute('aria-pressed',String(done));btn.textContent=done?'Complete ✓ · مكتملة':'Mark complete · أكمل الوحدة';const dot=document.querySelector(`[data-nav-module="${id}"] .status-dot`);if(dot)dot.textContent=done?'●':'○'});const pct=moduleIds.length?Math.round(completed.size/moduleIds.length*100):0;progressFill.forEach(x=>x.style.width=`${pct}%`);progressText.forEach(x=>x.textContent=`${completed.size} / ${moduleIds.length} modules · ${pct}%`);store.set('ec-completed',[...completed])}
moduleButtons.forEach(btn=>btn.addEventListener('click',()=>{const id=btn.dataset.module;completed.has(id)?completed.delete(id):completed.add(id);updateProgress()}));
function updateBookmarks(){bookmarkButtons.forEach(btn=>{const id=btn.dataset.module,a=bookmarks.has(id);btn.classList.toggle('bookmarked',a);btn.setAttribute('aria-pressed',String(a));btn.textContent=a?'★ Bookmarked · محفوظة':'☆ Bookmark · حفظ'});store.set('ec-bookmarks',[...bookmarks])}bookmarkButtons.forEach(btn=>btn.addEventListener('click',()=>{const id=btn.dataset.module;bookmarks.has(id)?bookmarks.delete(id):bookmarks.add(id);updateBookmarks()}));updateProgress();updateBookmarks();
const searchable=[...document.querySelectorAll('.searchable')];searchInput?.addEventListener('input',()=>{const q=searchInput.value.trim().toLocaleLowerCase();let n=0;searchable.forEach(s=>{const h=`${s.dataset.search||''} ${s.textContent}`.toLocaleLowerCase(),m=!q||h.includes(q);s.classList.toggle('search-hidden',!m);if(m)n++});if(searchStatus)searchStatus.textContent=q?`${n} matches · ${n} نتائج`:''});
document.querySelectorAll('.quiz-check').forEach(b=>b.addEventListener('click',()=>{const block=b.closest('.quiz-question'),sel=block.querySelector('input:checked'),fb=block.querySelector('.quiz-feedback');if(!sel){fb.textContent='Choose an answer first · اختر إجابة أولاً';fb.className='quiz-feedback incorrect';return}const ok=sel.value===block.dataset.answer;fb.textContent=ok?block.dataset.correct:block.dataset.incorrect;fb.className=`quiz-feedback ${ok?'correct':'incorrect'}`}));
function num(id){const v=parseFloat(document.getElementById(id)?.value);return Number.isFinite(v)?v:NaN}function output(id,t){const e=document.getElementById(id);if(e)e.textContent=t}const F=96485.33212,R=8.314462618;
document.getElementById('calcFaraday')?.addEventListener('submit',e=>{e.preventDefault();const I=num('farI'),t=num('farT')*60,M=num('farM'),n=num('farN'),eta=num('farEta')/100;if(!(I>=0&&t>=0&&M>0&&n>0&&eta>=0&&eta<=1))return output('farOut','Check inputs · راجع المدخلات');const m=eta*M*I*t/(n*F);output('farOut',`m = ${m.toPrecision(6)} g; Q = ${(I*t).toPrecision(6)} C`) });
document.getElementById('calcNernst')?.addEventListener('submit',e=>{e.preventDefault();const E0=num('nerE0'),n=num('nerN'),T=num('nerT'),Q=num('nerQ');if(!(n>0&&T>0&&Q>0))return output('nerOut','Check inputs · راجع المدخلات');const E=E0-R*T/(n*F)*Math.log(Q);output('nerOut',`E = ${E.toFixed(6)} V; correction = ${(E-E0).toFixed(6)} V`) });
document.getElementById('calcBV')?.addEventListener('submit',e=>{e.preventDefault();const i0=num('bvI0'),eta=num('bvEta')/1000,n=num('bvN'),aa=num('bvAa'),ac=num('bvAc'),T=298.15;if(!(i0>0&&n>0&&aa>0&&ac>0))return output('bvOut','Check inputs · راجع المدخلات');const i=i0*(Math.exp(aa*n*F*eta/(R*T))-Math.exp(-ac*n*F*eta/(R*T)));output('bvOut',`i = ${i.toPrecision(6)} mA cm⁻² at 298.15 K`) });
document.getElementById('calcCottrell')?.addEventListener('submit',e=>{e.preventDefault();const n=num('cotN'),A=num('cotA'),D=num('cotD'),c=num('cotC'),t=num('cotT');if(!(n>0&&A>0&&D>0&&c>0&&t>0))return output('cotOut','Check inputs · راجع المدخلات');const i=n*F*A*Math.sqrt(D)*c/Math.sqrt(Math.PI*t);output('cotOut',`i = ${(i*1000).toPrecision(6)} mA`) });
document.getElementById('calcCV')?.addEventListener('submit',e=>{e.preventDefault();const n=num('cvN'),A=num('cvA'),D=num('cvD'),c=num('cvC'),v=num('cvV'),T=298.15;if(!(n>0&&A>0&&D>0&&c>0&&v>0))return output('cvOut','Check inputs · راجع المدخلات');const i=.4463*n*F*A*c*Math.sqrt(n*F*v*D/(R*T));output('cvOut',`i_p = ${(i*1000).toPrecision(6)} mA (reversible model)`) });
document.getElementById('calcCapacity')?.addEventListener('submit',e=>{e.preventDefault();const n=num('capN'),M=num('capM');if(!(n>0&&M>0))return output('capOut','Check inputs · راجع المدخلات');output('capOut',`C_th = ${(n*F/(3.6*M)).toPrecision(6)} mAh g⁻¹`) });
function prepCanvas(canvas,h=420){if(!canvas)return null;const rect=canvas.getBoundingClientRect(),dpr=window.devicePixelRatio||1,w=Math.max(520,rect.width||700);canvas.width=w*dpr;canvas.height=h*dpr;canvas.style.height=`${h}px`;const ctx=canvas.getContext('2d');ctx.setTransform(dpr,0,0,dpr,0,0);ctx.fillStyle='#fff';ctx.fillRect(0,0,w,h);return{ctx,w,h}}
let curveData=[];function drawCurve(){const p=prepCanvas(document.getElementById('curveCanvas'));if(!p)return;const{ctx,w,h}=p,pad={l:76,r:24,t:40,b:62},model=document.getElementById('curveModel').value,p1=num('curveP1'),p2=num('curveP2');curveData=[];if(model==='bv'){for(let k=0;k<=160;k++){const x=-.2+.4*k/160,y=p1*(Math.exp(p2*F*x/(R*298.15))-Math.exp(-(1-p2)*F*x/(R*298.15)));curveData.push({x,y})}}else if(model==='nernst'){for(let k=0;k<=160;k++){const x=-6+12*k/160,y=p1-R*298.15/(Math.max(1,p2)*F)*Math.LN10*x;curveData.push({x,y})}}else{for(let k=0;k<=160;k++){const x=.01*Math.pow(10000,k/160),y=p1/Math.sqrt(x);curveData.push({x:Math.log10(x),y})}}
const xs=curveData.map(d=>d.x),ys=curveData.map(d=>d.y);let xmin=Math.min(...xs),xmax=Math.max(...xs),ymin=Math.min(...ys),ymax=Math.max(...ys);if(ymax===ymin){ymax+=1;ymin-=1}const my=(ymax-ymin)*.08;ymin-=my;ymax+=my;const X=x=>pad.l+(x-xmin)/(xmax-xmin)*(w-pad.l-pad.r),Y=y=>h-pad.b-(y-ymin)/(ymax-ymin)*(h-pad.t-pad.b);ctx.strokeStyle='#d8d2c4';ctx.font='12px Segoe UI';ctx.fillStyle='#596575';for(let i=0;i<=5;i++){const xv=xmin+(xmax-xmin)*i/5,yv=ymin+(ymax-ymin)*i/5;ctx.beginPath();ctx.moveTo(X(xv),pad.t);ctx.lineTo(X(xv),h-pad.b);ctx.stroke();ctx.fillText(xv.toFixed(2),X(xv)-14,h-pad.b+22);ctx.beginPath();ctx.moveTo(pad.l,Y(yv));ctx.lineTo(w-pad.r,Y(yv));ctx.stroke();ctx.fillText(yv.toPrecision(3),6,Y(yv)+4)}ctx.strokeStyle='#092b3a';ctx.lineWidth=2;ctx.beginPath();ctx.moveTo(pad.l,pad.t);ctx.lineTo(pad.l,h-pad.b);ctx.lineTo(w-pad.r,h-pad.b);ctx.stroke();ctx.strokeStyle='#0d7a72';ctx.lineWidth=3;ctx.beginPath();curveData.forEach((d,i)=>{i?ctx.lineTo(X(d.x),Y(d.y)):ctx.moveTo(X(d.x),Y(d.y))});ctx.stroke();ctx.fillStyle='#092b3a';ctx.font='14px Segoe UI';ctx.fillText(model==='bv'?'η / V':model==='nernst'?'log₁₀ Q':'log₁₀ t / s',w/2,h-18);ctx.save();ctx.translate(18,h/2+30);ctx.rotate(-Math.PI/2);ctx.fillText(model==='bv'?'i / relative units':model==='nernst'?'E / V':'i / relative units',0,0);ctx.restore();output('curveSummary',`${curveData.length} calculated points. Equation-based educational simulation; parameters: ${p1}, ${p2}.`) }
document.getElementById('curveForm')?.addEventListener('submit',e=>{e.preventDefault();drawCurve()});document.getElementById('downloadCurve')?.addEventListener('click',()=>{if(!curveData.length)drawCurve();const csv='x,y\n'+curveData.map(d=>`${d.x},${d.y}`).join('\n'),blob=new Blob([csv],{type:'text/csv'}),a=document.createElement('a');a.href=URL.createObjectURL(blob);a.download='electrochemistry_equation_simulation.csv';a.click();URL.revokeObjectURL(a.href)});
const navLinks=[...document.querySelectorAll('.sidebar a[href^="#"]')],observed=navLinks.map(a=>document.querySelector(a.getAttribute('href'))).filter(Boolean),observer=new IntersectionObserver(es=>{const v=es.filter(e=>e.isIntersecting).sort((a,b)=>b.intersectionRatio-a.intersectionRatio)[0];if(!v)return;navLinks.forEach(a=>a.classList.toggle('active',a.getAttribute('href')===`#${v.target.id}`))},{rootMargin:'-25% 0px -65% 0px',threshold:[0,.1,.5]});observed.forEach(x=>observer.observe(x));window.addEventListener('scroll',()=>backTop?.classList.toggle('show',window.scrollY>700));backTop?.addEventListener('click',()=>window.scrollTo({top:0,behavior:'smooth'}));window.addEventListener('resize',drawCurve);drawCurve();
})();